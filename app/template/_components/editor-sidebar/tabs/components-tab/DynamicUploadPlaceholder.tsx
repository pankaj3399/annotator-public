import { Upload } from 'lucide-react'

const DynamicUploadPlaceholder = ({ className }: { className?: string }) => {
  return <Upload className={className} />
}

export default DynamicUploadPlaceholder